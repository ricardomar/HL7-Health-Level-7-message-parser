function resultado=MSH_15(campo)
    
    campo=char(campo);

    tamanho=length(campo);
    tabela155={'AL', 'NE', 'ER', 'SU'};

    if tamanho<=2 && sum(strcmp(campo,tabela155))==1
        resultado=1;
    else
        resultado=0;
    end